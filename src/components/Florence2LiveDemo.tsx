// Thin adapter so the Florence-2 page keeps its existing import path.
// All real logic lives in the shared HFSpaceEmbed component now.

export { HFSpaceEmbed as Florence2LiveDemo } from "@/components/HFSpaceEmbed";
